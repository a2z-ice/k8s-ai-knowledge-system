apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-classic-pvc
  namespace: k8s-classic-ai
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
  volumeName: kafka-classic-pv
