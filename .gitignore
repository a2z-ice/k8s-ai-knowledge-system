# Docker bind-mount volumes (large runtime data — not committed)
data/

# Node
node_modules/
package-lock.json

# Playwright reports and test artifacts
playwright-report/
test-results/

# Docker Compose override (auto-generated by npm run screenshots)
docker-compose.override.yml

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
Icon?
ehthumbs.db
Thumbs.db

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
